{
  "name": "@agent-os/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "agentos": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.5",
    "commander": "^12.1.0",
    "eventsource": "^3.0.2",
    "inquirer": "^10.1.0",
    "ora": "^8.0.1",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@agent-os/shared": "workspace:*",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
